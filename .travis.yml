language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libboost-dev
      - g++-8
      - cmake

branches:
  only:
    - master

before_script:
  - cmake .

script:
  - make
