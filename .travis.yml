dist: xenial

language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libboost-program-options-dev
      - cmake

branches:
  only:
    - master

before_script:
  - cmake .

script:
  - make
